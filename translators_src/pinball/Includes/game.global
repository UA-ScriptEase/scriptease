/**
** Game.Global defines GameMap used to dynamically allocate memory 
** for game objects and whether or not the game has been started
** @author mfchurch
**/
#ifndef GAMEGLOBAL
#define GAMEGLOBAL
#include <string>
#include <map>
typedef std::map<std::string, std::string> ObjectMap;
typedef std::map<std::string, ObjectMap> GameMap;

GameMap gameData;
bool inGame = false;
#endif